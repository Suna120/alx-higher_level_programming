#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    vval = x
    for key, value in a_dictionary.items():
        if value == vval:
            del a_dictionary[key]
            break
print(a_dictionary)
